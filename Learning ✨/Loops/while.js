// // its synatx is this 
// while (condition of terminating the loop) {
//     // body of the loop
//   }

let subjects = ["maths", "english", "arts","science"]; //created and array 
let i = 0; //initialization of iteration variable
console.log("Printing the array of subjects");
while(i<subjects.length){ //while then in brackets we write condition to when terminate the loop with iteration variable 
    console.log(subjects[i]); //printing the value of array
    i++;// now we increment the value of iteration variable
}